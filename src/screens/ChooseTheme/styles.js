import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
    },
    themesDescription: {
        fontSize: 16,
        color: '#525252',
        padding: 15,
    },
    themeButton: {
        alignItems: 'center',
    }
});

export default styles;